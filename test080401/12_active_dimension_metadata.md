# Active-dimension sensitivity

The primary table now contains leakage-free results for `r=12/16/32/64` with five seeds and a `r=128` seed-11 smoke result. The separate `12_active_dimension_legacy_per_seed.csv` preserves reusable legacy `r=32/64` results for protocol comparison. The `r=128` row is not treated as a formal five-seed conclusion.
